# Vite Development Environment Configuration

# API后端地址
# Backend API target URL
VITE_API_TARGET=https://127.0.0.1:25774

# 如果使用HTTP后端，改为：
# If using HTTP backend, change to:
# VITE_API_TARGET=http://127.0.0.1:25774

# Base URL (通常不需要修改)
# Base URL (usually no need to change)
# VITE_BASE_URL=/
